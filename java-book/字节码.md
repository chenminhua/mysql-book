```
load 从局部变量加载值到栈上，
ldc 从常量池加载常量到栈上，
store 把值从栈中移走，存到局部变量中
dup 复制栈顶的值

invokestatic,
invokevirtual,
invokeinterface,
invokespecial,
invokedynamic 动态调用，是为非 java 语言准备的，目的是为了让动态类型语言能使用 jvm

new 为新对象分配内存
monitorenter 锁住对象
monitorexit 解锁对象
```
